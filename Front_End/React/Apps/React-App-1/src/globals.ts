import { Info } from "./types";

const postsData: Info[] = [];
const maxInfoCount = 50;
const minInfoCount = 10;
const emojiMax = 128591;
const emojiMin = 128512;

export { postsData, maxInfoCount, minInfoCount, emojiMax, emojiMin };
